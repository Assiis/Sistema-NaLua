services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: nalua
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: M@theus27062023
    ports:
      - "5432:5432"
    volumes:
      - nalua_pg:/var/lib/postgresql/data

volumes:
  nalua_pg: